---
phase: 13-new-game-types-drawing-bluffing
plan: 08
type: execute
wave: 5
depends_on:
  - 13-04
  - 13-05
  - 13-06
  - 13-07
files_modified: []
autonomous: false
requirements:
  - REQ-009
  - REQ-010
must_haves:
  truths:
    - "A complete Drawing round can be played: host starts game with drawing mix, artist sees canvas, non-artists guess by typing, correct guess scores points, reveal shows prompt word"
    - "A complete Bluffing round can be played: players submit fake answers, vote phase shows all answers, reveal shows who bluffed and who told truth"
    - "Mixed game type session plays through trivia + drawing + bluffing questions in one session without crashes"
    - "Both game types render correctly in Arabic RTL on mobile"
    - "TypeScript build passes with zero errors on both apps"
  artifacts:
    - path: "apps/web/.next"
      provides: "Production build artifact"
    - path: "apps/server/dist"
      provides: "Compiled server"
  key_links:
    - from: "host UI HostNewClient"
      to: "room:create gameMix payload"
      via: "Socket.io room:create"
      pattern: "gameMix"
    - from: "draw:stroke artist socket event"
      to: "canvas relay to non-artists"
      via: "socket.to(roomCode).emit"
      pattern: "draw:stroke"
---

<objective>
Verify that both new game types work end-to-end in a real browser session. This checkpoint confirms the Drawing and Bluffing mechanics are functional before Phase 13 is marked complete.

Purpose: Phase 13 delivers two complex real-time mechanics — human eyes on the actual UX is required to confirm stroke rendering, guess flow, bluffing submit/vote/reveal, and mixed-session progression all work.
Output: Human-verified sign-off that both game types are playable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-new-game-types-drawing-bluffing/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — TypeScript + production build</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the following automated checks in sequence. Fix any errors before proceeding:

1. TypeScript compile checks:
```bash
cd /c/shllahaV2/apps/server && npx tsc --noEmit
cd /c/shllahaV2/apps/web && npx tsc --noEmit
```

2. Next.js production build:
```bash
cd /c/shllahaV2/apps/web && npm run build 2>&1 | tail -20
```

3. Verify new socket handlers registered:
```bash
grep -n "registerDrawingHandlers\|registerBluffingHandlers" /c/shllahaV2/apps/server/src/socket/index.ts
```

4. Verify game engine routes DRAWING/BLUFFING:
```bash
grep -n "DRAWING\|BLUFFING\|startBluffingRound\|clearDrawingTimer" /c/shllahaV2/apps/server/src/socket/game.ts | head -20
```

5. Verify seed content exists in DB (after seed has run):
```bash
cd /c/shllahaV2/apps/server && npx ts-node -e "const {PrismaClient} = require('@prisma/client'); const p = new PrismaClient(); p.drawingPrompt.count().then(n => console.log('Drawing prompts:', n)); p.question.count({where:{type:'BLUFFING'}}).then(n => console.log('Bluffing questions:', n))"
```

All checks must pass (zero TS errors, build succeeds, grep finds handlers, DB shows 50+ of each) before the human checkpoint.
  </action>
  <verify>All 5 automated checks above produce zero errors / expected output</verify>
  <done>TypeScript compiles cleanly on both apps, Next.js build succeeds, handlers registered, seed data present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 13 complete implementation:
- Draw and Guess game type: canvas with stroke sync, guess input, speed-based scoring, artist bonus
- Bluffing game type: submit fake answers, vote phase, reveal with scores
- Mixed game type session: host selects trivia/drawing/bluffing counts at room creation
- Admin DrawingPrompt CRUD at /admin/drawing-prompts
- 50+ Arabic drawing prompts and 50+ bluffing questions seeded
  </what-built>
  <how-to-verify>
**Test A — Drawing round (requires 2 browser windows):**
1. Open host window: visit https://shlahlav2.vercel.app/host (or local http://localhost:3000/host)
2. At room creation, set game mix to: trivia=0, drawing=2, bluffing=0 → click "إنشاء غرفة"
3. Open player window: join the room code with a player name
4. Host: click "ابدأ اللعبة"
5. Player window designated as artist should see the drawing canvas with the Arabic prompt word visible
6. Other player windows should see the canvas (blank initially) and a guess text input
7. Artist draws a simple shape — verify strokes appear on non-artist screens in real time
8. Non-artist types the correct Arabic prompt word — verify they receive points + the room sees "أجاب صحيح!"
9. After 60 seconds OR host clicks "كشف" → verify all players see the prompt word revealed
10. Expected: drawing canvas works, stroke relay visible across windows, guess scores correctly

**Test B — Bluffing round:**
1. Create new room with game mix: trivia=0, drawing=0, bluffing=2
2. One player is secretly assigned the real answer (hidden from others)
3. All players submit their answers (fake or real)
4. Vote phase begins — all answers shown shuffled; players vote
5. Reveal: who was the bluffer vs truth-teller, point distribution shown
6. Expected: submit phase → vote phase → reveal phase all transition correctly; scores reflect bluffer votes and truth-finder bonus

**Test C — Mixed session:**
1. Create room with game mix: trivia=3, drawing=1, bluffing=1
2. Play through all 5 questions
3. Expected: trivia questions, one drawing round, one bluffing round all play in sequence without crashes

**Test D — Admin DrawingPrompt:**
1. Visit https://shlahlav2.vercel.app/admin/drawing-prompts (or local /admin/drawing-prompts)
2. Verify the list shows 50+ seeded prompts
3. Add a new prompt: text="طاولة", category="أشياء", difficulty="سهل"
4. Verify it appears in the list
5. Archive it — verify it disappears from the active list (or shows as archived if showArchived=1)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass, or describe which test failed and what you observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test browser→production | Functional testing against deployed environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-01 | Information Disclosure | prompt text in game:start payload | mitigate | Verify that player-facing question:start event shows '🎨 ارسم وخمّن' not the actual prompt word (only artist/host receive real text) |
| T-13-08-02 | Tampering | bluffing answer visibility | mitigate | Verify during submit phase that players cannot see each other's submitted answers; only revealed in vote phase |
</threat_model>

<verification>
All automated build checks pass + human sign-off on all 4 test scenarios.
</verification>

<success_criteria>
- TypeScript compiles with zero errors on both apps/server and apps/web
- Next.js production build succeeds
- Draw & Guess: strokes relay in real time, correct guess scores points, reveal shows prompt
- Bluffing: submit → vote → reveal flow completes, scores distributed correctly
- Mixed session: heterogeneous question types play in sequence without errors
- Admin DrawingPrompt CRUD: create and archive work
</success_criteria>

<output>
After completion, create `.planning/phases/13-new-game-types-drawing-bluffing/13-08-SUMMARY.md`
</output>
