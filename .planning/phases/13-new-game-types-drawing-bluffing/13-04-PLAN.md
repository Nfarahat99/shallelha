---
phase: 13-new-game-types-drawing-bluffing
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - apps/web/app/join/[roomCode]/game/DrawingCanvas.tsx
  - apps/web/app/join/[roomCode]/game/DrawingGuesserInput.tsx
  - apps/web/app/host/[roomCode]/game/HostDrawingView.tsx
  - apps/web/app/join/[roomCode]/game/PlayerGameScreen.tsx
  - apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx
autonomous: true
requirements:
  - REQ-009
  - REQ-010
must_haves:
  truths:
    - "Artist player sees a canvas with color picker, eraser, undo; can draw with mouse and touch"
    - "All other players see the canvas strokes in real-time and can type guesses"
    - "Host screen shows the live canvas and a count of correct guessers"
    - "When someone guesses correctly, a toast notification appears on all screens"
    - "draw:revealed transitions all screens to show the prompt word"
  artifacts:
    - path: "apps/web/app/join/[roomCode]/game/DrawingCanvas.tsx"
      provides: "Artist canvas with HTML5 Canvas API, color picker, eraser, undo"
      exports: ["DrawingCanvas"]
    - path: "apps/web/app/join/[roomCode]/game/DrawingGuesserInput.tsx"
      provides: "Spectator view: receives relayed strokes + text input for guessing"
      exports: ["DrawingGuesserInput"]
    - path: "apps/web/app/host/[roomCode]/game/HostDrawingView.tsx"
      provides: "Host view: live read-only canvas + guesser count + reveal button"
      exports: ["HostDrawingView"]
  key_links:
    - from: "apps/web/app/join/[roomCode]/game/PlayerGameScreen.tsx"
      to: "DrawingCanvas or DrawingGuesserInput"
      via: "question.type === 'DRAWING' branch"
      pattern: "DRAWING"
    - from: "apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx"
      to: "HostDrawingView"
      via: "question.type === 'DRAWING' branch"
      pattern: "DRAWING"
---

<objective>
Build the drawing game frontend: the artist's HTML5 canvas with real-time stroke emission, the guesser's read-only mirrored canvas with text input, and the host's oversight view. All use the socket events defined in Plan 02.

Purpose: Completes the player-facing ارسم وخمّن experience. This plan covers the full rendering layer.
Output: Three new components integrated into the existing question display flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-new-game-types-drawing-bluffing/CONTEXT.md
@.planning/phases/13-new-game-types-drawing-bluffing/13-02-SUMMARY.md

<interfaces>
<!-- Socket events from Plan 02 that these components use -->

Artist emits:
- `draw:stroke` — DrawingStroke: { x: number, y: number, color: string, size: number, type: 'start'|'move'|'end' }
- `draw:clear` — no payload
- `draw:guess` — { text: string }  [NOT used by artist; included for guesser]

Server emits to room:
- `draw:stroke` — DrawingStroke (relayed from artist, skipped for artist's own socket)
- `draw:clear` — no payload
- `draw:correct_guess` — { playerName: string, playerEmoji: string }
- `draw:revealed` — { promptText: string, artistId: string }

Host emits:
- `draw:reveal` — no payload

Existing socket from apps/web/lib/socket.ts — already connected, use the existing socket instance.

From apps/web/app/join/[roomCode]/game/PlayerGameScreen.tsx:
```tsx
// wrapper component, children receive question:start payload
// question.type can be 'MULTIPLE_CHOICE' | 'MEDIA_GUESSING' | 'FREE_TEXT' | 'DRAWING' | 'BLUFFING'
```

From apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx:
- Already handles MULTIPLE_CHOICE, MEDIA_GUESSING, FREE_TEXT
- Add DRAWING branch that renders <HostDrawingView />
- question payload shape: { text, options, timerDuration, type, mediaUrl?, drawingPromptId? }
- For DRAWING: `text` = prompt word (only visible on host screen until draw:revealed)

Tailwind + RTL: dir="rtl" on containers, Arabic text for labels
Design system: brand-950/brand-300 color palette, glass card pattern (bg-white/5 border border-white/10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build DrawingCanvas (artist) and DrawingGuesserInput (spectator)</name>
  <files>
    apps/web/app/join/[roomCode]/game/DrawingCanvas.tsx
    apps/web/app/join/[roomCode]/game/DrawingGuesserInput.tsx
  </files>
  <read_first>
    apps/web/app/join/[roomCode]/game/FreeTextInput.tsx
    apps/web/app/join/[roomCode]/game/VotingUI.tsx
  </read_first>
  <action>
Create apps/web/app/join/[roomCode]/game/DrawingCanvas.tsx — the artist's interactive canvas:

```tsx
'use client'
import { useRef, useEffect, useState, useCallback } from 'react'
import { socket } from '@/lib/socket'

interface DrawingCanvasProps {
  roomCode: string
  disabled?: boolean
}

const COLORS = ['#000000', '#ef4444', '#3b82f6', '#22c55e', '#f59e0b', '#a855f7', '#ec4899', '#ffffff']

export function DrawingCanvas({ roomCode, disabled = false }: DrawingCanvasProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const isDrawingRef = useRef(false)
  const historyRef = useRef<ImageData[]>([])
  const [color, setColor] = useState('#000000')
  const [size, setSize] = useState(6)
  const [isEraser, setIsEraser] = useState(false)

  // Normalize event coords to [0,1] relative to canvas
  function normalize(canvas: HTMLCanvasElement, clientX: number, clientY: number) {
    const rect = canvas.getBoundingClientRect()
    return {
      x: (clientX - rect.left) / rect.width,
      y: (clientY - rect.top) / rect.height,
    }
  }

  function getCtx() {
    const canvas = canvasRef.current
    return canvas ? canvas.getContext('2d') : null
  }

  function drawStroke(x: number, y: number, strokeColor: string, strokeSize: number, type: 'start' | 'move' | 'end') {
    const ctx = getCtx()
    const canvas = canvasRef.current
    if (!ctx || !canvas) return
    const px = x * canvas.width
    const py = y * canvas.height
    if (type === 'start') {
      // Save state for undo
      historyRef.current.push(ctx.getImageData(0, 0, canvas.width, canvas.height))
      ctx.beginPath()
      ctx.moveTo(px, py)
    } else if (type === 'move') {
      ctx.lineTo(px, py)
      ctx.strokeStyle = strokeColor
      ctx.lineWidth = strokeSize
      ctx.lineCap = 'round'
      ctx.lineJoin = 'round'
      ctx.stroke()
    } else {
      ctx.closePath()
    }
  }

  function emitStroke(x: number, y: number, type: 'start' | 'move' | 'end') {
    if (disabled) return
    const strokeColor = isEraser ? '#1a0a3e' : color // bg color for eraser
    socket.emit('draw:stroke', { x, y, color: strokeColor, size, type })
    drawStroke(x, y, strokeColor, size, type)
  }

  function handlePointerDown(e: React.PointerEvent<HTMLCanvasElement>) {
    if (disabled) return
    isDrawingRef.current = true
    ;(e.target as HTMLCanvasElement).setPointerCapture(e.pointerId)
    const { x, y } = normalize(e.currentTarget, e.clientX, e.clientY)
    emitStroke(x, y, 'start')
  }

  function handlePointerMove(e: React.PointerEvent<HTMLCanvasElement>) {
    if (!isDrawingRef.current || disabled) return
    const { x, y } = normalize(e.currentTarget, e.clientX, e.clientY)
    emitStroke(x, y, 'move')
  }

  function handlePointerUp(e: React.PointerEvent<HTMLCanvasElement>) {
    if (!isDrawingRef.current) return
    isDrawingRef.current = false
    const { x, y } = normalize(e.currentTarget, e.clientX, e.clientY)
    emitStroke(x, y, 'end')
  }

  function handleUndo() {
    const ctx = getCtx()
    const canvas = canvasRef.current
    if (!ctx || !canvas || historyRef.current.length === 0) return
    const prev = historyRef.current.pop()!
    ctx.putImageData(prev, 0, 0)
    socket.emit('draw:clear') // full clear + replay approach: simplest for now
  }

  function handleClear() {
    const ctx = getCtx()
    const canvas = canvasRef.current
    if (!ctx || !canvas) return
    historyRef.current = []
    ctx.clearRect(0, 0, canvas.width, canvas.height)
    socket.emit('draw:clear')
  }

  return (
    <div dir="rtl" className="flex flex-col gap-3 p-4">
      <canvas
        ref={canvasRef}
        width={600}
        height={400}
        className="w-full rounded-xl bg-brand-950/80 border border-white/20 touch-none cursor-crosshair"
        style={{ maxHeight: '55vw' }}
        onPointerDown={handlePointerDown}
        onPointerMove={handlePointerMove}
        onPointerUp={handlePointerUp}
        onPointerLeave={handlePointerUp}
      />
      {/* Toolbar */}
      <div className="flex flex-wrap gap-2 justify-between items-center">
        {/* Color swatches */}
        <div className="flex gap-1.5">
          {COLORS.map((c) => (
            <button
              key={c}
              aria-label={`لون ${c}`}
              onClick={() => { setColor(c); setIsEraser(false) }}
              className={`w-7 h-7 rounded-full border-2 transition-transform ${
                color === c && !isEraser ? 'border-white scale-125' : 'border-white/30'
              }`}
              style={{ backgroundColor: c }}
            />
          ))}
        </div>
        {/* Eraser + Undo + Clear */}
        <div className="flex gap-2">
          <button
            onClick={() => setIsEraser((e) => !e)}
            className={`px-3 py-1.5 rounded-lg text-sm font-medium transition-colors ${
              isEraser ? 'bg-white text-brand-950' : 'bg-white/10 text-white'
            }`}
          >ممحاة</button>
          <button
            onClick={handleUndo}
            className="px-3 py-1.5 rounded-lg bg-white/10 text-white text-sm font-medium"
          >تراجع</button>
          <button
            onClick={handleClear}
            className="px-3 py-1.5 rounded-lg bg-red-500/20 text-red-400 text-sm font-medium"
          >مسح الكل</button>
        </div>
      </div>
      {/* Size slider */}
      <div className="flex items-center gap-3">
        <span className="text-white/60 text-xs">حجم</span>
        <input
          type="range" min={2} max={30} value={size}
          onChange={(e) => setSize(Number(e.target.value))}
          className="flex-1 accent-brand-400"
        />
        <span className="text-white/60 text-xs w-6">{size}</span>
      </div>
    </div>
  )
}
```

Create apps/web/app/join/[roomCode]/game/DrawingGuesserInput.tsx — read-only mirrored canvas + text guess input:

```tsx
'use client'
import { useRef, useEffect, useState } from 'react'
import { socket } from '@/lib/socket'

interface DrawingGuesserInputProps {
  roomCode: string
  onCorrect?: (points: number) => void
}

export function DrawingGuesserInput({ roomCode, onCorrect }: DrawingGuesserInputProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const [guess, setGuess] = useState('')
  const [guessResult, setGuessResult] = useState<'correct' | 'wrong' | null>(null)
  const [guessedCorrectly, setGuessedCorrectly] = useState(false)
  const [toast, setToast] = useState<string | null>(null)

  function drawReceivedStroke(stroke: { x: number; y: number; color: string; size: number; type: 'start' | 'move' | 'end' }) {
    const canvas = canvasRef.current
    const ctx = canvas?.getContext('2d')
    if (!ctx || !canvas) return
    const px = stroke.x * canvas.width
    const py = stroke.y * canvas.height
    if (stroke.type === 'start') {
      ctx.beginPath(); ctx.moveTo(px, py)
    } else if (stroke.type === 'move') {
      ctx.lineTo(px, py)
      ctx.strokeStyle = stroke.color; ctx.lineWidth = stroke.size
      ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.stroke()
    } else { ctx.closePath() }
  }

  useEffect(() => {
    socket.on('draw:stroke', drawReceivedStroke)
    socket.on('draw:clear', () => {
      const canvas = canvasRef.current
      const ctx = canvas?.getContext('2d')
      if (ctx && canvas) ctx.clearRect(0, 0, canvas.width, canvas.height)
    })
    socket.on('draw:guess:result', ({ correct, points }: { correct: boolean; points?: number }) => {
      setGuessResult(correct ? 'correct' : 'wrong')
      if (correct) {
        setGuessedCorrectly(true)
        onCorrect?.(points ?? 0)
      }
      setTimeout(() => setGuessResult(null), 2000)
    })
    socket.on('draw:correct_guess', ({ playerName, playerEmoji }: { playerName: string; playerEmoji: string }) => {
      setToast(`${playerEmoji} ${playerName} خمّن الإجابة!`)
      setTimeout(() => setToast(null), 3000)
    })
    return () => {
      socket.off('draw:stroke', drawReceivedStroke)
      socket.off('draw:clear')
      socket.off('draw:guess:result')
      socket.off('draw:correct_guess')
    }
  }, [])

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!guess.trim() || guessedCorrectly) return
    socket.emit('draw:guess', { text: guess.trim() })
    setGuess('')
  }

  return (
    <div dir="rtl" className="flex flex-col gap-3 p-4">
      {toast && (
        <div className="bg-green-500/20 border border-green-400/30 rounded-xl px-4 py-2 text-green-300 text-sm text-center">
          {toast}
        </div>
      )}
      <canvas
        ref={canvasRef}
        width={600}
        height={400}
        className="w-full rounded-xl bg-brand-950/80 border border-white/20"
        style={{ maxHeight: '55vw' }}
      />
      {guessedCorrectly ? (
        <div className="bg-green-500/20 border border-green-400/30 rounded-xl p-4 text-center text-green-300 font-bold">
          أحسنت! خمّنت الإجابة الصحيحة
        </div>
      ) : (
        <form onSubmit={handleSubmit} className="flex gap-2">
          <input
            type="text"
            value={guess}
            onChange={(e) => setGuess(e.target.value)}
            placeholder="اكتب تخمينك..."
            maxLength={100}
            className={`flex-1 bg-white/10 border rounded-xl px-4 py-3 text-white placeholder-white/40 text-right outline-none focus:ring-2 focus:ring-brand-400 transition-colors ${
              guessResult === 'wrong' ? 'border-red-400' : 'border-white/20'
            }`}
          />
          <button
            type="submit"
            className="bg-brand-500 text-white px-4 py-3 rounded-xl font-bold hover:bg-brand-400 active:scale-95 transition-all"
          >إرسال</button>
        </form>
      )}
    </div>
  )
}
```

Wire into the existing player game screen. Read PlayerGameScreen.tsx and the component that renders question content (likely the page.tsx or a sub-component), then add a DRAWING branch:
- If `question.type === 'DRAWING'` and socket.data indicates current player is artist → render `<DrawingCanvas>`
- If `question.type === 'DRAWING'` and player is not artist → render `<DrawingGuesserInput>`
- The `artistPlayerId` is emitted with `question:start` payload for DRAWING questions (add this to the server's sendQuestion call as well — read game.ts to find sendQuestion and add artistPlayerId to the DRAWING payload)
  </action>
  <verify>cd /c/shllahaV2/apps/web && npx tsc --noEmit 2>&1 | head -30</verify>
  <acceptance_criteria>
- DrawingCanvas renders canvas with color picker (8 colors), eraser, undo, clear, size slider
- Touch events handled via Pointer Events API (works on mobile)
- DrawingGuesserInput mirrors strokes in real-time via draw:stroke socket listener
- Guesser input disabled after correct guess
- Toast shows when any player guesses correctly
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>DrawingCanvas and DrawingGuesserInput created and wired into player game screen</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build HostDrawingView and wire into QuestionDisplay</name>
  <files>
    apps/web/app/host/[roomCode]/game/HostDrawingView.tsx
    apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx
  </files>
  <read_first>
    apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx
    apps/web/app/host/[roomCode]/game/HostInGameControls.tsx
  </read_first>
  <action>
Create apps/web/app/host/[roomCode]/game/HostDrawingView.tsx:

```tsx
'use client'
import { useRef, useEffect, useState } from 'react'
import { socket } from '@/lib/socket'

interface HostDrawingViewProps {
  promptText: string // The word being drawn — visible ONLY to host
  roomCode: string
  onReveal: () => void
}

export function HostDrawingView({ promptText, roomCode, onReveal }: HostDrawingViewProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const [correctCount, setCorrectCount] = useState(0)
  const [revealed, setRevealed] = useState(false)

  function drawReceivedStroke(stroke: { x: number; y: number; color: string; size: number; type: 'start' | 'move' | 'end' }) {
    const canvas = canvasRef.current
    const ctx = canvas?.getContext('2d')
    if (!ctx || !canvas) return
    const px = stroke.x * canvas.width
    const py = stroke.y * canvas.height
    if (stroke.type === 'start') { ctx.beginPath(); ctx.moveTo(px, py) }
    else if (stroke.type === 'move') {
      ctx.lineTo(px, py)
      ctx.strokeStyle = stroke.color; ctx.lineWidth = stroke.size
      ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.stroke()
    } else { ctx.closePath() }
  }

  useEffect(() => {
    socket.on('draw:stroke', drawReceivedStroke)
    socket.on('draw:clear', () => {
      const canvas = canvasRef.current
      const ctx = canvas?.getContext('2d')
      if (ctx && canvas) ctx.clearRect(0, 0, canvas.width, canvas.height)
    })
    socket.on('draw:correct_guess', () => setCorrectCount((c) => c + 1))
    socket.on('draw:revealed', () => setRevealed(true))
    return () => {
      socket.off('draw:stroke', drawReceivedStroke)
      socket.off('draw:clear')
      socket.off('draw:correct_guess')
      socket.off('draw:revealed')
    }
  }, [])

  function handleReveal() {
    socket.emit('draw:reveal')
    setRevealed(true)
    onReveal()
  }

  return (
    <div dir="rtl" className="flex flex-col gap-4 p-6">
      {/* Host-only prompt label */}
      <div className="bg-white/5 border border-white/10 rounded-xl px-6 py-3 text-center">
        <p className="text-white/50 text-xs mb-1">الكلمة التي يرسمها اللاعب</p>
        <p className="text-white text-2xl font-bold tracking-wide">{promptText}</p>
      </div>

      {/* Live canvas */}
      <canvas
        ref={canvasRef}
        width={600}
        height={400}
        className="w-full rounded-xl bg-brand-950/80 border border-white/20"
        style={{ maxHeight: '40vh' }}
      />

      {/* Stats + Reveal */}
      <div className="flex items-center justify-between">
        <div className="bg-green-500/20 border border-green-400/30 rounded-xl px-4 py-2">
          <span className="text-green-300 text-sm font-bold">{correctCount} خمّن الإجابة</span>
        </div>
        {!revealed && (
          <button
            onClick={handleReveal}
            className="bg-brand-500 text-white px-6 py-2.5 rounded-xl font-bold hover:bg-brand-400 transition-colors"
          >
            كشف الإجابة
          </button>
        )}
      </div>
    </div>
  )
}
```

In apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx:
- Read the current file to understand the existing render branches
- Add DRAWING import: `import { HostDrawingView } from './HostDrawingView'`
- Add DRAWING branch in the render: if `question.type === 'DRAWING'`, render `<HostDrawingView promptText={question.text} roomCode={roomCode} onReveal={onReveal} />`
- The question.text for DRAWING questions IS the prompt word (set by game engine from DrawingPrompt.text)
- For other question types, existing behavior unchanged
  </action>
  <verify>cd /c/shllahaV2/apps/web && npx tsc --noEmit 2>&1 | head -30</verify>
  <acceptance_criteria>
- HostDrawingView shows prompt word prominently (host-only)
- Live canvas mirrors all draw:stroke events
- Correct guesser counter increments on draw:correct_guess
- "كشف الإجابة" button emits draw:reveal
- QuestionDisplay renders HostDrawingView for question.type === 'DRAWING'
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>HostDrawingView wired into QuestionDisplay; host can see prompt + live canvas + trigger reveal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | draw:stroke, draw:guess, draw:clear, draw:reveal from browser |
| server→client | Relayed strokes, correct guess notifications, reveal event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | prompt text | mitigate | Prompt text sent only in draw:revealed event; HostDrawingView receives promptText from server via question:start (host socket only) — not leaked to player sockets |
| T-13-04-02 | Tampering | canvas injection | mitigate | Canvas draws using coordinate data only; no innerHTML or eval; stroke color validated hex regex on server |
| T-13-04-03 | DoS | stroke flood on client | accept | Socket rate limiter on server limits incoming stroke rate; client canvas rendering is lightweight |
</threat_model>

<verification>
1. `cd apps/web && npx tsc --noEmit` — zero errors
2. `cd apps/web && npx next build 2>&1 | tail -10` — builds cleanly
3. Visual: DrawingCanvas renders with color swatches, slider, undo/clear controls
4. Visual: HostDrawingView shows prompt text block, canvas, correct count
</verification>

<success_criteria>
- Artist can draw with mouse and touch on mobile
- Guesser sees strokes in real-time on mirrored canvas
- Host sees prompt word + live canvas + reveal button
- DRAWING branch integrated into QuestionDisplay and PlayerGameScreen
- Web app TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/13-new-game-types-drawing-bluffing/13-04-SUMMARY.md`
</output>
