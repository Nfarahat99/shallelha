---
phase: 12-user-profiles-persistent-leaderboards
plan: 05
type: execute
wave: 3
depends_on: [12-01]
files_modified:
  - apps/web/app/leaderboard/page.tsx
  - apps/web/app/leaderboard/LeaderboardClient.tsx
  - apps/web/app/api/leaderboard/route.ts
  - apps/web/tests/leaderboard/queries.test.ts
autonomous: true
requirements:
  - AC-008-7

must_haves:
  truths:
    - "GET /api/leaderboard?period=alltime returns JSON array of top 50 players with rank, name, avatarEmoji, totalGamesPlayed, winCount"
    - "GET /api/leaderboard?period=weekly returns top 50 players scoped to current ISO week (Monday..Sunday)"
    - "GET /api/leaderboard?category=catId filters results to that category"
    - "/leaderboard page renders a toggle (all-time / this week) and a ranked list of players"
    - "Unauthenticated users can view the leaderboard (public route)"
  artifacts:
    - path: "apps/web/app/api/leaderboard/route.ts"
      provides: "GET endpoint: period + category query params, $queryRaw aggregation on PlayerGameResult"
      exports: ["GET"]
    - path: "apps/web/app/leaderboard/page.tsx"
      provides: "RSC page that fetches leaderboard data server-side and passes to client component"
    - path: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      provides: "Client component with period/category toggle and ranked list"
  key_links:
    - from: "apps/web/app/leaderboard/LeaderboardClient.tsx"
      to: "/api/leaderboard"
      via: "fetch with period and category params on toggle change"
      pattern: "fetch.*api/leaderboard"
    - from: "apps/web/app/api/leaderboard/route.ts"
      to: "prisma.$queryRaw"
      via: "SQL aggregation on PlayerGameResult with JOIN to User"
      pattern: "queryRaw"
---

<objective>
Create the global leaderboard: a public `/leaderboard` page with all-time and weekly rankings, backed by a Prisma $queryRaw aggregation API route.

Purpose: The global leaderboard is the social retention anchor — players return weekly to check their ranking. Weekly scope creates urgency; all-time provides long-term pride.

Output:
- GET /api/leaderboard?period=alltime|weekly&category=optionalId
- /leaderboard RSC page with LeaderboardClient toggle + ranked list
- Top 50 by winCount (all-time) or wins this ISO week (weekly)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-user-profiles-persistent-leaderboards/12-RESEARCH.md

<interfaces>
<!-- Prisma models available after 12-01 migration -->

model User {
  id               String    @id @default(cuid())
  displayName      String?
  avatarEmoji      String?   @db.VarChar(10)
  avatarConfig     Json?
  totalGamesPlayed Int       @default(0)
  winCount         Int       @default(0)
  bestStreak       Int       @default(0)
  favoriteCategory String?
  gameResults      PlayerGameResult[]
}

model PlayerGameResult {
  id            String      @id @default(cuid())
  gameSessionId String
  userId        String?                         -- nullable after 12-01
  playerName    String
  score         Int
  rank          Int
  isWinner      Boolean     @default(false)
  createdAt     DateTime    @default(now())
  gameSession   GameSession @relation(...)
  @@index([userId, createdAt])
  @@index([isWinner, createdAt])
}

model GameSession {
  id           String    @id @default(cuid())
  roomCode     String
  categoryId   String?
  categoryName String?
  playedAt     DateTime  @default(now())
  playerCount  Int
  winnerId     String?
}

<!-- API response shape -->
interface LeaderboardRow {
  rank: number
  userId: string
  displayName: string | null
  avatarEmoji: string | null
  avatarConfig: unknown   // Json — pass through as-is to client
  wins: number
  gamesPlayed: number
  winRate: number         // wins / gamesPlayed * 100, rounded to 1 decimal
}

<!-- Period logic -->
// all-time: aggregate over all PlayerGameResult rows where userId IS NOT NULL
// weekly: aggregate over rows where createdAt >= Monday of current ISO week (UTC)
//
// ISO week Monday calculation (UTC):
//   const now = new Date()
//   const dayOfWeek = now.getUTCDay() // 0=Sun, 1=Mon ... 6=Sat
//   const daysToMonday = (dayOfWeek + 6) % 7
//   const monday = new Date(now)
//   monday.setUTCDate(now.getUTCDate() - daysToMonday)
//   monday.setUTCHours(0, 0, 0, 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/leaderboard route with $queryRaw aggregation</name>
  <files>apps/web/app/api/leaderboard/route.ts</files>
  <action>
Create `apps/web/app/api/leaderboard/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { prisma } from '@/lib/prisma'
import { Prisma } from '@prisma/client'

export const dynamic = 'force-dynamic'

function getMondayUTC(): Date {
  const now = new Date()
  const dayOfWeek = now.getUTCDay() // 0=Sun
  const daysToMonday = (dayOfWeek + 6) % 7
  const monday = new Date(now)
  monday.setUTCDate(now.getUTCDate() - daysToMonday)
  monday.setUTCHours(0, 0, 0, 0)
  return monday
}

interface LeaderboardRow {
  rank: bigint
  userId: string
  displayName: string | null
  avatarEmoji: string | null
  avatarConfig: unknown
  wins: bigint
  gamesPlayed: bigint
}

export async function GET(request: NextRequest) {
  const { searchParams } = request.nextUrl
  const period = searchParams.get('period') ?? 'alltime'
  const category = searchParams.get('category') ?? null

  // Input validation
  if (period !== 'alltime' && period !== 'weekly') {
    return NextResponse.json({ error: 'Invalid period' }, { status: 400 })
  }
  // category: alphanumeric + hyphen/underscore only (cuid format)
  const safeCategory = category?.replace(/[^a-zA-Z0-9_-]/g, '').slice(0, 40) ?? null

  try {
    // Build date filter clause
    const dateFilter: Prisma.Sql = period === 'weekly'
      ? Prisma.sql`AND pgr."createdAt" >= ${getMondayUTC()}`
      : Prisma.empty

    // Build category filter clause
    const categoryFilter: Prisma.Sql = safeCategory
      ? Prisma.sql`AND gs."categoryId" = ${safeCategory}`
      : Prisma.empty

    const rows = await prisma.$queryRaw<LeaderboardRow[]>`
      SELECT
        ROW_NUMBER() OVER (ORDER BY COUNT(*) FILTER (WHERE pgr."isWinner" = true) DESC, COUNT(*) DESC) AS rank,
        u."id"            AS "userId",
        u."displayName"   AS "displayName",
        u."avatarEmoji"   AS "avatarEmoji",
        u."avatarConfig"  AS "avatarConfig",
        COUNT(*) FILTER (WHERE pgr."isWinner" = true) AS wins,
        COUNT(*)                                        AS "gamesPlayed"
      FROM "PlayerGameResult" pgr
      JOIN "User"        u  ON u."id"  = pgr."userId"
      JOIN "GameSession" gs ON gs."id" = pgr."gameSessionId"
      WHERE pgr."userId" IS NOT NULL
        ${dateFilter}
        ${categoryFilter}
      GROUP BY u."id", u."displayName", u."avatarEmoji", u."avatarConfig"
      HAVING COUNT(*) > 0
      ORDER BY wins DESC, "gamesPlayed" DESC
      LIMIT 50
    `

    const result = rows.map((row, idx) => ({
      rank: idx + 1,
      userId: row.userId,
      displayName: row.displayName,
      avatarEmoji: row.avatarEmoji,
      avatarConfig: row.avatarConfig,
      wins: Number(row.wins),
      gamesPlayed: Number(row.gamesPlayed),
      winRate: row.gamesPlayed > 0n
        ? Math.round((Number(row.wins) / Number(row.gamesPlayed)) * 1000) / 10
        : 0,
    }))

    return NextResponse.json(result, {
      headers: { 'Cache-Control': 'public, s-maxage=60, stale-while-revalidate=300' },
    })
  } catch (err) {
    console.error('[leaderboard] query failed:', err)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

IMPORTANT: `prisma.$queryRaw` with tagged template literals (using `Prisma.sql`) prevents SQL injection — do NOT use string concatenation for the query.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep "leaderboard/route" | head -5 || echo "TypeScript check done"</automated>
  </verify>
  <done>
- apps/web/app/api/leaderboard/route.ts exists and exports GET
- TypeScript compiles without errors for this file
- $queryRaw uses Prisma.sql tagged template — no string concatenation
- period validation rejects non-alltime/weekly values
- Cache-Control header set for CDN caching
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /leaderboard page and LeaderboardClient component</name>
  <files>apps/web/app/leaderboard/page.tsx, apps/web/app/leaderboard/LeaderboardClient.tsx</files>
  <action>
**File 1 — `apps/web/app/leaderboard/page.tsx`** (RSC, no 'use client'):

```typescript
import { Suspense } from 'react'
import type { Metadata } from 'next'
import { LeaderboardClient } from './LeaderboardClient'

export const metadata: Metadata = {
  title: 'لوحة المتصدرين — شعللها',
  description: 'أفضل اللاعبين على شعللها',
}

// Fetch all-time leaderboard server-side for initial render (SSR)
async function fetchLeaderboard(period: 'alltime' | 'weekly' = 'alltime') {
  const baseUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'
  const res = await fetch(`${baseUrl}/api/leaderboard?period=${period}`, {
    next: { revalidate: 60 },
  })
  if (!res.ok) return []
  return res.json()
}

export default async function LeaderboardPage() {
  const initialData = await fetchLeaderboard('alltime')

  return (
    <main dir="rtl" className="min-h-dvh bg-gradient-to-b from-gray-950 via-purple-950 to-gray-900 px-4 py-8">
      <div className="max-w-2xl mx-auto w-full flex flex-col gap-6">
        <h1 className="text-3xl font-black text-white text-center">لوحة المتصدرين</h1>
        <Suspense fallback={<div className="text-white/50 text-center py-8">جاري التحميل...</div>}>
          <LeaderboardClient initialData={initialData} />
        </Suspense>
      </div>
    </main>
  )
}
```

**File 2 — `apps/web/app/leaderboard/LeaderboardClient.tsx`:**

```typescript
'use client'

import { useState, useTransition } from 'react'

interface LeaderboardRow {
  rank: number
  userId: string
  displayName: string | null
  avatarEmoji: string | null
  avatarConfig: unknown
  wins: number
  gamesPlayed: number
  winRate: number
}

interface Props {
  initialData: LeaderboardRow[]
}

const PERIOD_LABELS = {
  alltime: 'كل الأوقات',
  weekly: 'هذا الأسبوع',
} as const

export function LeaderboardClient({ initialData }: Props) {
  const [period, setPeriod] = useState<'alltime' | 'weekly'>('alltime')
  const [rows, setRows] = useState<LeaderboardRow[]>(initialData)
  const [isPending, startTransition] = useTransition()

  async function handlePeriodChange(newPeriod: 'alltime' | 'weekly') {
    if (newPeriod === period) return
    setPeriod(newPeriod)
    startTransition(async () => {
      const res = await fetch(`/api/leaderboard?period=${newPeriod}`)
      if (res.ok) {
        const data = await res.json()
        setRows(data)
      }
    })
  }

  const RANK_STYLES: Record<number, string> = {
    1: 'bg-yellow-500/20 border-yellow-500/40 text-yellow-300',
    2: 'bg-gray-400/20 border-gray-400/40 text-gray-300',
    3: 'bg-orange-600/20 border-orange-600/40 text-orange-300',
  }

  const RANK_MEDALS: Record<number, string> = { 1: '🥇', 2: '🥈', 3: '🥉' }

  return (
    <div className="flex flex-col gap-4">
      {/* Period toggle */}
      <div className="flex gap-2 justify-center">
        {(['alltime', 'weekly'] as const).map((p) => (
          <button
            key={p}
            onClick={() => handlePeriodChange(p)}
            className={`px-4 py-2 rounded-full text-sm font-semibold transition-colors ${
              period === p
                ? 'bg-brand-600 text-white'
                : 'bg-white/10 text-white/60 hover:bg-white/20'
            }`}
          >
            {PERIOD_LABELS[p]}
          </button>
        ))}
      </div>

      {/* Leaderboard list */}
      <div className={`flex flex-col gap-2 transition-opacity ${isPending ? 'opacity-50' : 'opacity-100'}`}>
        {rows.length === 0 && (
          <p className="text-white/50 text-center py-12">لا توجد بيانات بعد</p>
        )}
        {rows.map((row) => {
          const rowStyle = RANK_STYLES[row.rank] ?? 'bg-white/5 border-white/10 text-white/60'
          const medal = RANK_MEDALS[row.rank]
          return (
            <div
              key={row.userId}
              className={`flex items-center gap-3 rounded-xl border px-4 py-3 ${rowStyle}`}
            >
              {/* Rank */}
              <span className="w-8 text-lg font-black text-center shrink-0">
                {medal ?? `#${row.rank}`}
              </span>
              {/* Avatar (emoji fallback until PlayerAvatar wired) */}
              <span className="text-2xl shrink-0" aria-hidden="true">
                {row.avatarEmoji ?? '🎮'}
              </span>
              {/* Name + stats */}
              <div className="flex flex-col flex-1 min-w-0">
                <span className="font-semibold text-white truncate">
                  {row.displayName ?? 'لاعب مجهول'}
                </span>
                <span className="text-xs text-white/50">
                  {row.wins} فوز من {row.gamesPlayed} — {row.winRate}%
                </span>
              </div>
            </div>
          )
        })}
      </div>
    </div>
  )
}
```

NOTE: The leaderboard currently uses avatarEmoji as the avatar display. After 12-07 (profile enhancement) upgrades the profile to use avatarConfig, add a follow-up to wire PlayerAvatar here — but avatarConfig is already in the API response, so it is available when needed. Do NOT add a dependency on 12-07 now; the emoji fallback is intentional.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | grep -E "leaderboard/page|LeaderboardClient|error TS" | head -10 || echo "TypeScript check done"</automated>
  </verify>
  <done>
- apps/web/app/leaderboard/page.tsx exists as RSC (no 'use client')
- apps/web/app/leaderboard/LeaderboardClient.tsx exists with period toggle
- Both files pass TypeScript compilation
- Page has SSR initial data load with 60s revalidate
- Client component fetches fresh data on period toggle
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test stub for leaderboard queries</name>
  <files>apps/web/tests/leaderboard/queries.test.ts</files>
  <action>
Create `apps/web/tests/leaderboard/queries.test.ts` with a minimal test.todo stub:

```typescript
import { describe, test } from 'vitest'

describe('Leaderboard API', () => {
  test.todo('GET /api/leaderboard returns 200 with valid JSON array')
  test.todo('GET /api/leaderboard?period=invalid returns 400')
  test.todo('GET /api/leaderboard?period=weekly scopes to current week')
  test.todo('leaderboard results are limited to 50 entries')
})
```

This satisfies the RESEARCH.md Wave 0 Validation Architecture requirement. Full test implementation deferred to 12-09 smoke tests.
  </action>
  <verify>
    <automated>ls apps/web/tests/leaderboard/queries.test.ts && echo "Stub exists"</automated>
  </verify>
  <done>
- apps/web/tests/leaderboard/queries.test.ts exists with test.todo stubs
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/leaderboard | period and category query params come from untrusted client |
| /api/leaderboard → PostgreSQL | $queryRaw executes parameterized SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | SQL injection via period/category params | mitigate | period validated to enum; category sanitized with regex [a-zA-Z0-9_-]; Prisma.sql tagged template prevents injection |
| T-12-05-02 | Information Disclosure | leaderboard exposes userId | accept | userId is a non-secret cuid used for linking profiles; no PII exposed beyond displayName (which users choose publicly) |
| T-12-05-03 | Denial of Service | leaderboard query on large dataset | mitigate | LIMIT 50 on query; Cache-Control s-maxage=60 so CDN absorbs repeated requests |
</threat_model>

<verification>
```bash
# TypeScript check
cd apps/web && npx tsc --noEmit 2>&1 | grep "leaderboard" | head -10

# Verify files exist
ls apps/web/app/api/leaderboard/route.ts
ls apps/web/app/leaderboard/page.tsx
ls apps/web/app/leaderboard/LeaderboardClient.tsx

# Verify $queryRaw is used (not raw string concat)
grep "queryRaw" apps/web/app/api/leaderboard/route.ts
grep "Prisma.sql" apps/web/app/api/leaderboard/route.ts
```
</verification>

<success_criteria>
- GET /api/leaderboard?period=alltime returns 200 with JSON array (when DB has data)
- GET /api/leaderboard?period=invalid returns 400
- /leaderboard page renders without errors (npx next build passes)
- $queryRaw uses only Prisma.sql tagged template literals — zero string concatenation
- Cache-Control: public, s-maxage=60 header present on API response
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-profiles-persistent-leaderboards/12-05-SUMMARY.md`
</output>
