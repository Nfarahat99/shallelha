---
phase: 11-growth-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/host/[roomCode]/WhatsAppShareButton.tsx
  - apps/web/app/host/[roomCode]/QRCodeDisplay.tsx
  - apps/web/app/host/[roomCode]/HostDashboard.tsx
autonomous: true
requirements: [REQ-001]
must_haves:
  truths:
    - "Host lobby shows a WhatsApp share button with Arabic label"
    - "Host lobby shows a QR code encoding the join URL"
    - "Tapping WhatsApp button opens wa.me with pre-written Arabic message"
    - "QR code is at least 200x200px and scannable"
  artifacts:
    - path: "apps/web/app/host/[roomCode]/WhatsAppShareButton.tsx"
      provides: "WhatsApp share button component"
      contains: "wa.me"
    - path: "apps/web/app/host/[roomCode]/QRCodeDisplay.tsx"
      provides: "QR code display component"
      contains: "qrcode"
    - path: "apps/web/app/host/[roomCode]/HostDashboard.tsx"
      provides: "Lobby section with WhatsApp + QR integrated"
      contains: "WhatsAppShareButton"
  key_links:
    - from: "WhatsAppShareButton.tsx"
      to: "wa.me URL"
      via: "window.open"
      pattern: "wa\\.me"
    - from: "QRCodeDisplay.tsx"
      to: "qrcode npm"
      via: "toDataURL"
      pattern: "toDataURL"
    - from: "HostDashboard.tsx"
      to: "WhatsAppShareButton.tsx"
      via: "import"
      pattern: "WhatsAppShareButton"
---

<objective>
Add WhatsApp share button and QR code display to the host lobby screen.

Purpose: Enable hosts to share the room join link via WhatsApp (one-tap) and display a scannable QR code for in-person players — the two primary viral distribution mechanisms.
Output: Two new components + updated HostDashboard lobby section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-growth-foundation-landing-page-anonymous-rooms-sharing/11-UI-SPEC.md
@.planning/phases/11-growth-foundation-landing-page-anonymous-rooms-sharing/11-CONTEXT.md
@apps/web/app/host/[roomCode]/HostDashboard.tsx

<interfaces>
<!-- HostDashboard.tsx is a client component with phases: lobby, pre-game, playing, ended -->
<!-- Lobby phase renders: room code card, player list, host controls -->
<!-- joinUrl computed from window.location.origin + '/join/' + roomCode -->
<!-- roomCode prop available as string -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhatsAppShareButton component</name>
  <files>apps/web/app/host/[roomCode]/WhatsAppShareButton.tsx</files>
  <action>
Create a client component ('use client') that accepts props: { roomCode: string, joinUrl: string }.

1. WhatsApp message template (from CONTEXT.md):
   `انضم لجلسة شعللها 🎮\nكود الغرفة: ${roomCode}\n${joinUrl}`

2. onClick handler:
   - On mobile (detect via navigator.userAgent or window.innerWidth < 768): use `whatsapp://send?text=` deep link
   - On desktop: use `https://wa.me/?text=` URL
   - Both use encodeURIComponent(message)
   - Open via window.open(url, '_blank')

3. Style per UI-SPEC:
   - `w-full flex items-center justify-center gap-2 py-3 rounded-2xl bg-green-600 hover:bg-green-500 active:bg-green-700 text-white font-bold text-base transition-all min-h-[44px]`
   - WhatsApp SVG icon (white, 20x20) inline — use the standard WhatsApp logo path

4. Label: "مشاركة عبر واتساب"

Do NOT import any external icon library. Use an inline SVG for the WhatsApp icon.
  </action>
  <verify>
    <automated>cd C:/shllahaV2 && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <done>WhatsAppShareButton component exists, renders green button with WhatsApp icon and Arabic label, opens wa.me URL on click.</done>
</task>

<task type="auto">
  <name>Task 2: Create QRCodeDisplay component and integrate both into HostDashboard lobby</name>
  <files>apps/web/app/host/[roomCode]/QRCodeDisplay.tsx, apps/web/app/host/[roomCode]/HostDashboard.tsx</files>
  <action>
**QRCodeDisplay component:**

1. Install qrcode package: `npm install qrcode` and `npm install -D @types/qrcode` in apps/web
2. Create client component ('use client') accepting props: { joinUrl: string }
3. Use useEffect + useState to generate QR:
   ```
   import QRCode from 'qrcode'
   const [dataUrl, setDataUrl] = useState<string>('')
   useEffect(() => {
     QRCode.toDataURL(joinUrl, { width: 200, margin: 1, color: { dark: '#000', light: '#fff' } })
       .then(setDataUrl)
   }, [joinUrl])
   ```
4. Render: `<div className="bg-white p-3 rounded-xl mx-auto w-fit">` containing `<img src={dataUrl} alt="QR" width={200} height={200} className="rounded-lg" />`
5. Caption below: `<p className="text-xs text-white/50 text-center mt-2">امسح للانضمام</p>`
6. Show a loading placeholder (small spinner) while dataUrl is empty

**HostDashboard integration:**

1. Import WhatsAppShareButton and QRCodeDisplay at top of HostDashboard.tsx
2. In the lobby section (where status === 'lobby' or equivalent), compute joinUrl:
   `const joinUrl = typeof window !== 'undefined' ? window.location.origin + '/join/' + roomCode : ''`
   (This may already exist — reuse if so)
3. Insert QRCodeDisplay inside or below the room code card
4. Insert WhatsAppShareButton below the QR code, above the player list
5. Placement order per UI-SPEC: Header -> Room code card (now includes QR) -> WhatsApp button -> Player list -> Host controls

Do NOT modify the ended, pre-game, or playing sections of HostDashboard.
  </action>
  <verify>
    <automated>cd C:/shllahaV2/apps/web && npx tsc --noEmit --pretty 2>&1 | head -10</automated>
  </verify>
  <done>QR code (200x200, white bg, scannable) and WhatsApp share button visible on host lobby screen. QR encodes /join/{code} URL. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → WhatsApp | URL passed to wa.me must be properly encoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | T (Tampering) | WhatsApp URL | mitigate | encodeURIComponent on entire message text prevents injection into wa.me URL |
| T-11-05 | I (Info Disclosure) | QR code | accept | QR only encodes the public join URL which is already visible as text on the lobby screen |
</threat_model>

<verification>
- TypeScript compiles without errors
- WhatsAppShareButton renders on lobby screen
- QRCodeDisplay renders 200x200 QR code on lobby screen
- QR code encodes correct join URL
</verification>

<success_criteria>
Host lobby screen displays WhatsApp share button (green, Arabic label) and QR code (200x200, scannable). Tapping WhatsApp button opens pre-written Arabic message. QR code encodes /join/{code} URL.
</success_criteria>

<output>
After completion, create `.planning/phases/11-growth-foundation-landing-page-anonymous-rooms-sharing/11-03-SUMMARY.md`
</output>
