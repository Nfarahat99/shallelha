---
phase: 11-growth-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
autonomous: true
requirements: [REQ-004]
must_haves:
  truths:
    - "Landing page primary CTA reads 'ابدأ لعبة' (not 'أنشئ غرفة')"
    - "Landing page secondary CTA reads 'انضم للعبة' (not 'عندك كود؟ انضم')"
    - "OG meta tags include Arabic title and description"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Updated CTA copy and OG meta"
      contains: "ابدأ لعبة"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "/host"
      via: "Primary CTA link"
      pattern: "ابدأ لعبة"
---

<objective>
Update the existing landing page CTA button text and OG meta tags per UI-SPEC.

Purpose: The landing page already exists with hero, explainer, features, and stats. Only the CTA copy and meta tags need updating — NOT a full rebuild.
Output: Updated page.tsx with correct Arabic CTA text and SEO meta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-growth-foundation-landing-page-anonymous-rooms-sharing/11-UI-SPEC.md
@apps/web/app/page.tsx

<interfaces>
<!-- Landing page already exists (509 lines) with hero, 3-step explainer, features, stats, CTA -->
<!-- Has existing metadata export — update to match UI-SPEC -->
<!-- Uses AnimatedCounter from ./LandingClient -->
<!-- Host CTA requires Google OAuth (no anonymous host per CONTEXT.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing page CTA copy and OG meta tags</name>
  <files>apps/web/app/page.tsx</files>
  <action>
1. Find the primary host CTA button text and change from "أنشئ غرفة" (or current text) to "ابدأ لعبة" per UI-SPEC. The button links to /host and requires Google OAuth — this stays as-is.

2. Find the secondary player CTA button text and change from "عندك كود؟ انضم" (or current text) to "انضم للعبة" per UI-SPEC. This links to /join.

3. Update the metadata export (or generateMetadata if dynamic) to include:
   - title: "شعللها 🎮 — لعبة الأسئلة الجماعية"
   - description: "ابدأ لعبة أسئلة مع أصدقائك في ثوانٍ — بدون تسجيل"
   - openGraph: title, description, type: 'website'
   - twitter: card: 'summary_large_image'

Keep all existing page structure, animations, sections, and styles intact. This is a copy-only change + meta update.

Style per UI-SPEC:
- Primary CTA: bg-white text-brand-600 hover:bg-white/90 rounded-2xl px-8 py-4 text-lg font-bold min-h-[56px]
- Secondary CTA: bg-white/15 border border-white/30 text-white hover:bg-white/25 rounded-2xl px-8 py-4 text-lg font-bold min-h-[56px]

Only update styles if they differ from current — check before changing.
  </action>
  <verify>
    <automated>cd C:/shllahaV2 && npx next build --no-lint 2>&1 | tail -5</automated>
  </verify>
  <done>Landing page shows "ابدأ لعبة" as primary CTA and "انضم للعبة" as secondary CTA. OG meta tags present with Arabic title and description. Page builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static page content change, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | S (Spoofing) | Landing page CTA | accept | Host CTA still requires Google OAuth — no anonymous room creation per user decision |
</threat_model>

<verification>
- Page builds successfully
- CTA text matches UI-SPEC copy
- OG meta tags render correctly
</verification>

<success_criteria>
Landing page CTA buttons display the correct Arabic copy. OG meta tags are set for SEO. No regressions to existing page layout.
</success_criteria>

<output>
After completion, create `.planning/phases/11-growth-foundation-landing-page-anonymous-rooms-sharing/11-02-SUMMARY.md`
</output>
