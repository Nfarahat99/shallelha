---
phase: 10-ugc-question-packs-shareable-cards
plan: 05
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - apps/web/app/admin/packs/page.tsx
  - apps/web/app/admin/packs/actions.ts
  - apps/web/app/admin/layout.tsx
autonomous: true
requirements:
  - UGC-01
must_haves:
  truths:
    - "Admin can see all PENDING packs in a dedicated queue tab"
    - "Admin can approve or reject a pack with a single click"
    - "Approved packs immediately appear in the marketplace"
    - "Rejected packs show a rejection reason visible to the pack creator"
    - "Admin pack queue is protected by the existing ADMIN_SESSION_TOKEN cookie gate"
  artifacts:
    - path: "apps/web/app/admin/packs/page.tsx"
      provides: "Admin pending packs queue page"
    - path: "apps/web/app/admin/packs/actions.ts"
      provides: "approvePack and rejectPack Server Actions"
  key_links:
    - from: "apps/web/app/admin/packs/actions.ts"
      to: "PATCH /packs/:id/status on Express"
      via: "fetch(BACKEND_URL + '/packs/' + packId + '/status')"
    - from: "apps/web/app/admin/layout.tsx"
      to: "existing admin cookie session guard"
      via: "existing middleware/layout pattern from Phase 7"
---

<objective>
Add a "Pending Packs" queue tab to the existing admin dashboard so the admin can review, approve, or reject submitted community packs.

Purpose: Without admin approval, no community pack can go live. This plan closes the approval loop using the existing admin session pattern from Phase 7/9 — no new auth system.
Output: /admin/packs page with approve/reject actions, wired into existing admin layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ugc-question-packs-shareable-cards/CONTEXT.md
@.planning/phases/10-ugc-question-packs-shareable-cards/10-01-SUMMARY.md
@apps/web/app/admin/layout.tsx
</context>

<interfaces>
<!-- Admin auth pattern from Phase 7/9 -->
Admin cookie session: ADMIN_SESSION_TOKEN env var
Pattern: admin layout.tsx or middleware checks for the cookie; if absent → redirect('/admin/login')
Server Actions in admin pages: check cookies().get('admin_session')?.value === process.env.ADMIN_SESSION_TOKEN

<!-- Pack CRUD from Plan 01 -->
GET /packs?status=PENDING — list pending packs (need to expose this on the API; Plan 01's GET /packs filters by APPROVED; admin needs PENDING)
Note: Plan 01's PATCH /packs/:id/status already accepts any PackStatus — admin can use it directly.

Admin needs to extend Plan 01's GET /packs endpoint to accept ?status= param (add note for executor to update packsRouter.get to accept status filter — currently hardcoded to APPROVED).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Admin pending packs page with approve/reject actions</name>
  <files>
    apps/web/app/admin/packs/page.tsx
    apps/web/app/admin/packs/actions.ts
    apps/web/app/admin/layout.tsx
  </files>
  <action>
First, read apps/web/app/admin/layout.tsx and apps/web/app/admin/questions/page.tsx (or whichever admin page exists) to understand the exact admin auth pattern used. Follow the same pattern.

Also update apps/server/src/routes/packs.ts GET /packs to accept optional ?status= query param:
- If status param is provided and valid (matches PackStatus enum), filter by that status
- If status=PENDING (admin use) — include all packs of that status (no APPROVED filter override)
- Default behavior (no status param) stays as APPROVED only

Create apps/web/app/admin/packs/actions.ts ('use server'):
- Both actions verify admin cookie: `cookies().get('admin_session')?.value !== process.env.ADMIN_SESSION_TOKEN` → throw new Error('Unauthorized')

- approvePack(packId: string):
  - PATCH /packs/:id/status with { status: 'APPROVED' }
  - revalidatePath('/admin/packs')
  - revalidatePath('/packs') — so marketplace updates immediately
  - Return { success: true } or { error: string }

- rejectPack(packId: string, reason: string):
  - PATCH /packs/:id/status with { status: 'REJECTED', rejectionReason: reason }
  - Note: rejectionReason field needs to be added to Pack model (add it to the schema change noted here — executor must also add rejectionReason String? to Pack in schema.prisma and run migration)
  - revalidatePath('/admin/packs')
  - Return { success: true } or { error: string }

Create apps/web/app/admin/packs/page.tsx:
- Server Component + force-dynamic
- Admin auth check (same pattern as other admin pages)
- Fetch GET /packs?status=PENDING from backend
- Render: page title "مراجعة الباقات المرسلة", count badge "X باقة قيد الانتظار"
- For each pending pack: card with pack name, category, language, question count, creator handle, "اعتمد" (approve) button, "ارفض" (reject) button
- Reject button opens an inline form/dialog for rejection reason before submitting
- Both buttons call their respective Server Actions via form action
- Empty state: "لا توجد باقات قيد المراجعة ✓"

Update apps/web/app/admin/layout.tsx (or wherever the admin nav lives):
- Add "الباقات" link to the admin navigation sidebar/tabs pointing to /admin/packs
- Add pending count badge: fetch GET /packs?status=PENDING in the layout, show count if > 0

Also add rejectionReason String? field to Pack model in apps/server/prisma/schema.prisma and run:
`cd apps/server && npx prisma migrate dev --name add-pack-rejection-reason`
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/web && npx next build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>next build passes. /admin/packs page compiles. Server Actions use admin cookie check. approvePack and rejectPack are exported from actions.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire approved pack status back to creator's My Packs view</name>
  <files>
    apps/web/app/packs/my-packs/page.tsx
  </files>
  <action>
Update apps/web/app/packs/my-packs/page.tsx (created in Plan 02):
- Fetch GET /packs/mine?userId={session.user.id} — this already returns all statuses
- For packs with status REJECTED: show the rejectionReason field beneath the status badge (Arabic: "سبب الرفض: {reason}")
- For packs with status APPROVED: show a green "مقبولة ✓" badge and a "عرض في السوق" link to /packs/[packId]
- For packs with status PENDING: show yellow "قيد المراجعة ⏳" badge and disable the edit button

This ensures pack creators know the result of admin review without requiring a separate notification system.
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/web && npx tsc --noEmit 2>&1 | grep "my-packs" | head -10</automated>
  </verify>
  <done>my-packs page shows rejectionReason for REJECTED packs. APPROVED packs link to the marketplace. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → approvePack Server Action | Admin-only action must verify cookie |
| GET /packs?status=PENDING | Sensitive: reveals all pending packs including non-public content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Elevation of Privilege | approvePack/rejectPack Server Actions without auth check | mitigate | Both actions check cookies().get('admin_session')?.value === process.env.ADMIN_SESSION_TOKEN; throw 'Unauthorized' if mismatch |
| T-10-05-02 | Information Disclosure | GET /packs?status=PENDING exposes PENDING pack content | mitigate | This endpoint should only be called from admin Server Components (server-side fetch with admin cookie); add note in packsRouter: PENDING status filter should require admin authentication — implement basic check in Plan 01's packs route when status=PENDING |
</threat_model>

<verification>
- /admin/packs loads and shows PENDING packs (requires admin cookie)
- approvePack transitions pack to APPROVED — pack appears on /packs marketplace within 1 minute
- rejectPack transitions pack to REJECTED with reason
- /packs/my-packs shows rejectionReason for REJECTED packs
- Unauthenticated access to /admin/packs redirects to admin login
- next build passes
</verification>

<success_criteria>
- Admin approves a pack from /admin/packs — pack appears in marketplace within 1 minute (per R1 acceptance criteria)
- Rejection reason visible to pack creator on My Packs page
- Admin nav shows pending count badge when packs are awaiting review
</success_criteria>

<output>
After completion, create `.planning/phases/10-ugc-question-packs-shareable-cards/10-05-SUMMARY.md`
</output>
