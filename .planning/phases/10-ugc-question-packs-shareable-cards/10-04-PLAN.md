---
phase: 10-ugc-question-packs-shareable-cards
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - apps/web/app/packs/page.tsx
  - apps/web/app/packs/[packId]/page.tsx
  - apps/web/app/host/new/page.tsx
  - apps/web/app/host/new/actions.ts
  - apps/web/components/packs/PackCard.tsx
  - apps/web/components/packs/PackFilters.tsx
autonomous: false
requirements:
  - UGC-02
must_haves:
  truths:
    - "Host can browse APPROVED packs filtered by category and language on /packs"
    - "Pack detail page shows name, description, question count, creator handle, play count"
    - "Host can click 'Play this pack' and enter the room creation flow with that pack pre-selected"
    - "Official packs (admin-curated) are visually badged differently from community packs"
    - "Marketplace works on mobile: RTL, touch-friendly cards"
  artifacts:
    - path: "apps/web/app/packs/page.tsx"
      provides: "Pack marketplace browse page with filters"
    - path: "apps/web/app/packs/[packId]/page.tsx"
      provides: "Pack detail page with Play this pack CTA"
    - path: "apps/web/components/packs/PackCard.tsx"
      provides: "Reusable pack card component"
    - path: "apps/web/components/packs/PackFilters.tsx"
      provides: "Category/language filter bar (client component)"
  key_links:
    - from: "apps/web/app/packs/page.tsx"
      to: "GET /packs on Express server"
      via: "fetch(BACKEND_URL + '/packs?category=...')"
    - from: "apps/web/app/host/new/page.tsx"
      to: "Pack selection pre-fill"
      via: "searchParams.packId → pre-select pack in room creation form"
---

<objective>
Build the Pack Marketplace: /packs browse page with category/language filters, pack detail pages, and integration with the host room creation flow so starting a game with a community pack takes under 60 seconds.

Purpose: Hosts need a discovery surface for community packs. This plan adds the browse/search/filter UI and wires "Play this pack" into the game start flow.
Output: /packs marketplace, /packs/[packId] detail, updated /host/new with pack selector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ugc-question-packs-shareable-cards/CONTEXT.md
@.planning/phases/10-ugc-question-packs-shareable-cards/10-01-SUMMARY.md
@apps/web/app/host/[roomCode]/game/HostGameScreen.tsx
</context>

<interfaces>
<!-- From Plan 01 Pack CRUD API -->
GET /packs — query params: ?category=, ?language=
Response: PackSummary[] where PackSummary = {
  id, name, description, category, language, difficulty, status,
  createdBy, creatorHandle, playCount, rating, questionCount, createdAt
}

GET /packs/:id — returns Pack with questions[]

Existing room creation flow (host):
- /host/new or /host page initiates room — need to check current route structure
- Pack selection: when packId is provided via searchParams, pre-fill the game config

Category names from existing DB:
  رمضان، محبوبين، رياضة، أفلام، ثقافة عامة، خليجي

Official pack detection:
  Pack.createdBy === 'admin' OR Pack.creatorHandle === 'الإدارة' — badge as "رسمي ✓"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pack marketplace page, PackCard component, and PackFilters</name>
  <files>
    apps/web/app/packs/page.tsx
    apps/web/app/packs/[packId]/page.tsx
    apps/web/components/packs/PackCard.tsx
    apps/web/components/packs/PackFilters.tsx
  </files>
  <action>
Create apps/web/components/packs/PackCard.tsx ('use client' — for hover state):
- Props: { pack: PackSummary }
- Card layout: rounded-xl, bg-gray-900/60, border border-white/10, p-4, hover:border-brand-500/50
- Top line: pack name (Arabic, font-bold text-white, dir="rtl") + official badge if admin-created ("✓ رسمي" in brand color)
- Middle: description (2-line clamp, text-white/60, dir="rtl")
- Stats row: "X سؤال" | "X تشغيل" | category tag | language badge (عربي / English / ثنائي)
- Footer: creator handle (text-white/40, "بواسطة @handle") + "▶ العب" button (brand color)
- Clicking card navigates to /packs/[packId]; clicking "العب" navigates to /host/new?packId={id}

Create apps/web/components/packs/PackFilters.tsx ('use client'):
- Props: { categories: string[], onFilter: (params: { category?: string, language?: string }) => void }
- Horizontal scroll filter chips: "الكل" + each category name + language buttons (عربي / English)
- Active chip: bg-brand-600 text-white; inactive: bg-white/10 text-white/70
- On chip click: calls onFilter; URL param updates via router.push (useRouter)

Create apps/web/app/packs/page.tsx (Server Component + 'force-dynamic'):
- Fetch categories from backend (GET /categories or use hardcoded list from CONTEXT.md defaults)
- Fetch GET /packs?category=&language= using searchParams
- Render: page header "باقات الأسئلة" (Arabic, RTL), PackFilters client component, grid of PackCard components
- Grid: 2-col on mobile, 3-col on md+, gap-4
- Empty state: "لا توجد باقات بعد في هذه الفئة"
- Add "إنشاء باقة جديدة" button → /packs/create (show only if user is authenticated)
- SEO: export const metadata with Arabic title/description

Create apps/web/app/packs/[packId]/page.tsx (Server Component):
- Fetch GET /packs/:id — 404 page if not found
- Render: pack name (large, RTL), description, metadata (category, language, difficulty, play count, rating)
- Question list preview: first 3 questions shown, rest hidden with "اطلع على باقي الأسئلة" toggle (client island)
- Creator section: "بواسطة @handle" + official badge
- CTA: "▶ العب هذه الباقة" → /host/new?packId={id} (large brand-colored button)
- Back link → /packs
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/web && npx next build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>next build passes. /packs and /packs/[packId] compile. PackCard and PackFilters compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pack selection into host room creation flow</name>
  <files>
    apps/web/app/host/new/page.tsx
    apps/web/app/host/new/actions.ts
  </files>
  <action>
Read the current host room creation page to understand its structure. It may be at /app/host/page.tsx or /app/host/new/page.tsx — check both.

Modify the room creation flow to accept a pre-selected pack:
- If ?packId= is in searchParams, fetch GET /packs/:id and display "باقة محددة: {name}" with a remove button
- If no pack selected, show the existing question count selector (preserve existing behavior)
- Pack selection stored in form state — passed to the createRoom Server Action

Modify createRoom action (in apps/web/app/host/new/actions.ts or wherever it currently lives):
- Accept optional packId in form data
- If packId provided: store packId in the Redis room state alongside game config
- Server must read packId from room state and serve questions from PackQuestion table instead of Question table

For Redis room state extension: add packId field to the room config stored in Redis. The game server (apps/server/src/game/game.ts) must be updated to read pack questions when packId is set — but this cross-server wiring belongs in a separate note (mark as TODO in the createRoom action with a comment: "TODO: game server reads pack questions via packId in room config — see apps/server game.ts").

Focus this task on the UI wiring only: pack pre-selection display, form integration, Redis room config extension with packId field.
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/web && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>TypeScript compiles. Room creation form shows selected pack name. Pack ID passed through to Redis room config. No regression in existing host flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Pack Marketplace (/packs): category/language filter chips, pack cards with stats, official badge. Pack detail page (/packs/[packId]): full info + "Play this pack" CTA. Room creation flow updated with pack pre-selection.
  </what-built>
  <how-to-verify>
    1. Navigate to /packs — verify cards render with Arabic names, stats, category chips
    2. Click a category chip — verify pack list filters
    3. Click a pack card — verify detail page loads with name, description, question count
    4. Click "العب هذه الباقة" — verify redirects to /host/new?packId={id} with pack shown as pre-selected
    5. Verify the page works on mobile (narrow viewport, RTL, touch targets ≥ 44px)
    6. Verify the marketplace loads within 2 seconds on a normal connection
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /packs page fetch | Server Component makes backend call — no user data exposed |
| ?packId= query param | User-supplied packId passed to room creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | ?packId= could be any pack ID — host could play a non-approved pack | mitigate | createRoom Server Action fetches pack from API; if status !== APPROVED, reject with Arabic error "هذه الباقة غير متاحة" |
| T-10-04-02 | Information Disclosure | Pack detail page shows all questions — could reveal answers | accept | Questions are quiz questions — their content is not sensitive; revealing them before the game is not a security issue |
</threat_model>

<verification>
- /packs loads APPROVED packs only (status filter working)
- Category and language filter chips update the displayed packs
- Pack detail page shows full pack info
- "العب هذه الباقة" → /host/new?packId= works
- Mobile layout: 2-col grid, RTL text, touch-friendly
- next build passes without errors
</verification>

<success_criteria>
- Host discovers and starts a game with a community pack in under 60 seconds (per R2 acceptance criteria)
- Pack browsing works on mobile: RTL layout, touch-friendly, no horizontal overflow
- Empty state shown when no approved packs exist for a category
</success_criteria>

<output>
After completion, create `.planning/phases/10-ugc-question-packs-shareable-cards/10-04-SUMMARY.md`
</output>
