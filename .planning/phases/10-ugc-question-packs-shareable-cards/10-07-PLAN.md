---
phase: 10-ugc-question-packs-shareable-cards
plan: 07
type: execute
wave: 3
depends_on:
  - 10-01-PLAN.md
  - 10-06-PLAN.md
files_modified:
  - apps/server/src/game/game.types.ts
  - apps/server/src/game/game.ts
  - apps/web/app/join/[roomCode]/PlayerJoin.tsx
  - apps/web/app/join/[roomCode]/components/FreezeOpponentOverlay.tsx
  - apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx
  - apps/web/app/host/[roomCode]/game/HostInGameControls.tsx
  - apps/web/tests/e2e/phase10-ux-wins.spec.ts
autonomous: true
requirements:
  - UGC-01
must_haves:
  truths:
    - "Player sees their live rank + score delta after each question answer is confirmed"
    - "Player screen shows 'X من Y أجابوا' answer count progress during question phase"
    - "Frozen player sees a full-screen 'أنت مجمّد ❄️' overlay for the duration of the freeze"
    - "Existing lifelines, scoring, and game flow are not broken"
  artifacts:
    - path: "apps/server/src/game/game.types.ts"
      provides: "Extended LeaderboardEntry with rankDelta; extended GameState with answerCount/playerCount"
    - path: "apps/server/src/game/game.ts"
      provides: "Updated leaderboard broadcast with rankDelta; updated question payload with answerCount"
    - path: "apps/web/app/join/[roomCode]/components/FreezeOpponentOverlay.tsx"
      provides: "Full-screen freeze overlay shown to frozen player"
    - path: "apps/web/tests/e2e/phase10-ux-wins.spec.ts"
      provides: "Playwright E2E tests for all 3 UX wins"
  key_links:
    - from: "apps/server/src/game/game.ts"
      to: "socket.emit('leaderboard', ...)"
      via: "rankDelta added to each LeaderboardEntry before broadcast"
    - from: "apps/server/src/game/game.ts"
      to: "socket.emit('question:state', ...)"
      via: "answerCount / playerCount added to payload during question phase"
    - from: "apps/web/app/join/[roomCode]/PlayerJoin.tsx"
      to: "FreezeOpponentOverlay"
      via: "frozenCurrentQ === true → render FreezeOpponentOverlay fullscreen"
---

<objective>
Implement the 3 Quick UX Wins from R5: live rank after each question, answer count progress, and freeze opponent overlay. Add Playwright E2E tests covering all three.

Purpose: These fixes lift the player experience score from 4.6/10 to a functional engagement loop — players know their rank, know others are answering, and feel the drama of being frozen.
Output: Extended type definitions, updated game.ts broadcast logic, two new/updated UI components, E2E test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ugc-question-packs-shareable-cards/CONTEXT.md
@apps/server/src/game/game.types.ts
@apps/web/app/host/[roomCode]/game/HostGameScreen.tsx
</context>

<interfaces>
<!-- Current type definitions to extend (from game.types.ts) -->
```typescript
export interface LeaderboardEntry {
  id: string
  name: string
  emoji: string
  score: number
  rank: number
  streak: number
}

export interface GameState {
  phase: 'pre-game' | 'question' | 'reveal' | 'leaderboard' | 'ended' | 'voting'
  currentQuestion: number
  totalQuestions: number
  // ... existing fields
}

export interface PlayerGameState {
  // ... existing fields
  frozenCurrentQ: boolean
  answeredCurrentQ: boolean
}
```

Additions needed:
```typescript
// LeaderboardEntry extension
export interface LeaderboardEntry {
  // ... existing fields
  rankDelta: number  // positive = improved rank, negative = dropped, 0 = same
}

// GameState extension for answer progress
export interface GameState {
  // ... existing fields
  answerCount: number     // how many players answered current question
  playerCount: number     // total active players
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend type definitions and game.ts broadcast logic</name>
  <files>
    apps/server/src/game/game.types.ts
    apps/server/src/game/game.ts
  </files>
  <behavior>
    - LeaderboardEntry.rankDelta: number — calculated as previousRank - currentRank (positive = improved)
    - GameState.answerCount: number — count of players who submitted an answer this question
    - GameState.playerCount: number — count of active (non-disconnected) players
    - rankDelta calculation: compare current leaderboard ranking to previous question's ranking per player
    - answerCount incremented on each player:answer event; reset to 0 at question start
    - Both answerCount and playerCount emitted in real-time as players answer (incremental update, not just at reveal)
  </behavior>
  <action>
Update apps/server/src/game/game.types.ts:
- Add `rankDelta: number` to LeaderboardEntry
- Add `answerCount: number` and `playerCount: number` to GameState (or QuestionPayload if that's where real-time question state lives)

Update apps/server/src/game/game.ts:
1. rankDelta logic:
   - Maintain a `previousRankings: Map<playerId, number>` at room level (reset each game)
   - After building the leaderboard array, compute rankDelta = (previousRank ?? currentRank) - currentRank
   - Update previousRankings with current ranks after each leaderboard broadcast
   - Emit rankDelta as part of each LeaderboardEntry in the 'leaderboard' socket event

2. answerCount logic:
   - Add `answerCount: number` to room game state (reset to 0 when new question starts)
   - Increment answerCount in the player:answer handler (after validation, before processing)
   - Emit 'question:progress' event to the room: { answerCount, playerCount } after each increment
   - playerCount = count of players in room with status !== 'disconnected'

3. Verify no regression: existing scoring, streak, lifeline logic untouched. Only adding new fields to outgoing payloads and new event.
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/server && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All existing server tests still pass. LeaderboardEntry.rankDelta present in type. question:progress event emitted. No regression.</done>
</task>

<task type="auto">
  <name>Task 2: Player UI updates + Playwright E2E tests</name>
  <files>
    apps/web/app/join/[roomCode]/PlayerJoin.tsx
    apps/web/app/join/[roomCode]/components/FreezeOpponentOverlay.tsx
    apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx
    apps/web/tests/e2e/phase10-ux-wins.spec.ts
  </files>
  <action>
FreezeOpponentOverlay — create apps/web/app/join/[roomCode]/components/FreezeOpponentOverlay.tsx:
- Full-screen overlay: fixed inset-0 z-50, bg-blue-950/90 backdrop-blur-sm
- Centered content: ❄️ (large emoji, text-8xl), "أنت مجمّد" (text-3xl text-blue-200 dir="rtl"), "لا يمكنك الإجابة على هذا السؤال" (text-lg text-blue-300/80 dir="rtl")
- Animated: pulsing border/glow effect using Tailwind animate-pulse on a ring element
- No dismiss button — overlay clears automatically when frozenCurrentQ becomes false

Update apps/web/app/join/[roomCode]/PlayerJoin.tsx:
1. FreezeOpponentOverlay: import and render FreezeOpponentOverlay when gameState.frozenCurrentQ === true
2. Live rank display: listen to 'leaderboard' events — find own entry, show rank + rankDelta:
   - Find player's own LeaderboardEntry after leaderboard event
   - Show in answer confirmation feedback: "المركز #{rank}" + delta badge (▲N green if improved, ▼N red if dropped)
   - Display in the answer confirmation state (after answeredCurrentQ=true, before next question)
3. Answer count progress: listen to 'question:progress' events — show "X من Y أجابوا" counter:
   - Render counter on the question view (not answer confirmed state) so players see others answering
   - Format: "٣ من ٨ أجابوا" using Arabic-Eastern numerals via toLocaleString('ar-EG')
   - Counter is subtle (text-white/50, small text) — not distracting from the question

Update apps/web/app/host/[roomCode]/game/QuestionDisplay.tsx:
- Show same "X من Y أجابوا" counter on the host screen during question phase
- Position: top-right or below timer — visible to host but secondary to the question content

Create apps/web/tests/e2e/phase10-ux-wins.spec.ts (Playwright):
- Test 1: Rank delta shown after question
  - Setup: 2 players in room, complete one question, verify player 1 sees rank badge in answer feedback
- Test 2: Answer count progress
  - Setup: 2 players, start question, have player 1 answer — verify "١ من ٢ أجابوا" appears
- Test 3: Freeze overlay
  - Setup: player 1 uses freeze lifeline on player 2, verify player 2 sees freeze overlay full-screen
  - Verify overlay clears after question advances

Use Playwright's page.locator and expect patterns matching existing E2E test patterns in apps/web/tests/.
  </action>
  <verify>
    <automated>cd /c/shllahaV2/apps/web && npx tsc --noEmit 2>&1 | grep -E "PlayerJoin|FreezeOpponent|QuestionDisplay" | head -10</automated>
  </verify>
  <done>TypeScript compiles. FreezeOpponentOverlay renders when frozenCurrentQ is true. Rank delta badge visible in answer confirmation. Answer count counter renders during question phase. E2E test file created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client socket events | New events: question:progress (answerCount, playerCount) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Information Disclosure | answerCount reveals how many players answered — minor game info | accept | This is intentional UX — players knowing others answered reduces "dead silence" anxiety |
| T-10-07-02 | Tampering | rankDelta could be manipulated via client — but it comes from server | accept | rankDelta computed entirely server-side from authoritative game state — client cannot influence it |
</threat_model>

<verification>
- `npx vitest run` in apps/server: all tests pass (including existing 91 tests)
- TypeScript compiles in apps/web with no errors
- FreezeOpponentOverlay renders when frozenCurrentQ=true, hidden when false
- rankDelta badge shows positive (green) and negative (red) values correctly
- Answer count counter shows "X من Y أجابوا" in Arabic numerals
- Playwright E2E test file exists at apps/web/tests/e2e/phase10-ux-wins.spec.ts
</verification>

<success_criteria>
- Live rank delta visible after each question answer (per R5 acceptance criteria)
- Answer count counter "٣ من ٨ أجابوا" shown on player screen during question phase
- Frozen player sees full-screen "أنت مجمّد ❄️" overlay with no confusion
- All 3 UX wins covered by Playwright E2E tests
- Zero regression in existing game mechanics (lifelines, scoring, streak)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ugc-question-packs-shareable-cards/10-07-SUMMARY.md`
</output>
